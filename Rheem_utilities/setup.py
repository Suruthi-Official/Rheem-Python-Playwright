# setup.py for python_test_elevate_utilities
from setuptools import setup, find_packages

setup(
    name='Rheem_utilities',
    version='0.1',
    packages=find_packages(),
    install_requires=[],  # You can add dependencies here if needed
    description='Utility functions and helpers for the elevate framework',
    author='suruthi',
    author_email='your.email@example.com',
)
